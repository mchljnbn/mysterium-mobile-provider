# Signing config for personal custom builds on this fork.
# Upstream build.gradle.kts reads these from local.properties; the CI
# workflow (or your own build) copies them into android/local.properties.
storeFile=release.keystore
storePassword=mystnodes2026
keyAlias=mystnodes
keyPassword=mystnodes2026
